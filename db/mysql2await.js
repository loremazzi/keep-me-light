/* let mysql = require('mysql2/promise');
let AsyncConn = await mysql.createConnection({
    host: "localhost",
    user: process.env.USER,
    password: process.env.MySQL,
    database: process.env.DATABASE,
 });

 module.exports = AsyncConn;
 */